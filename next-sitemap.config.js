/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'https://dreamacademy.id', // Ganti dengan URL situs Anda
    generateRobotsTxt: true,       // (optional) Men-generate robots.txt
    sitemapSize: 7000,             // Ukuran sitemap, default adalah 5000
    changefreq: 'daily',           // Frekuensi perubahan halaman
    priority: 0.7,                 // Prioritas URL di peta situs
  }
  